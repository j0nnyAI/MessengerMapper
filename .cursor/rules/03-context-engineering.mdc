---
description: In-session context engineering rules. Auto-attached on vault context paths.
globs: ["**/MessengerMapper-VAULT/04-CONTEXT/**", "**/MessengerMapper-VAULT/11-HANDOFFS/**"]
---

<!--
FILE: .cursor/rules/03-context-engineering.mdc
PURPOSE: In-session HANDOFF updates and vault append-only constraints.
NOTE: The globs above contain MessengerMapper tokens. This rule will NOT auto-attach
      until initialize-project.py has been run and tokens substituted. If you are seeing
      this comment in a live project, init has not completed.
-->

# Context engineering — MessengerMapper

## In-session updates
Update the active state file as understanding changes:
- Roadblocks/pivots: update `THE TECHNICAL NARRATIVE` in `HANDOFF.md` immediately.
- Roadmap row completion: update status + Session notes in `16-ROADMAPS/` via `../THE-NEXUS/03-SKILLS/update-roadmap.md` (do not check HANDOFF.md §4 checkboxes).
- Mid-session thread swap / reset: run COMPILE phase first, then teardown.

## Append-only vault sectors
Never edit historical logs in:
- `MessengerMapper-VAULT/00-LEDGERS/`
- `MessengerMapper-VAULT/05-MEMORY/`
- `MessengerMapper-VAULT/11-HANDOFFS/`
- `99-ARCHIVE/` everywhere.
