---
name: remediation-gap-watch
description: **Roadmap (Phase B overlay)** — `gaps on` / `enable gaps` / `watch gaps` / `find gaps` (no scope) → § **Mandatory cascade**; buffer on blast-radius traversals; **flush before row `quality`**. Operator does not re-paste watch directive. **Not** `gap analysis on <scope>`. Off: `gaps off`.
allowed-tools: [Read, Write, Edit, Bash, Glob]
---

<!--
FILE: .claude/skills/remediation-gap-watch/SKILL.md
PURPOSE: Project-local wrapper for the canonical NEXUS skill.
LINKS: ../../../THE-NEXUS/03-SKILLS/remediation-gap-watch.md
GENERATED: by sync-assets-to-template.py. Safe to hand-edit, but a re-run with --force will overwrite.
-->

# Skill: Remediation Gap Watch

Wraps `../../../THE-NEXUS/03-SKILLS/remediation-gap-watch.md`. Read the canonical file for the full protocol, quality bar, and anti-patterns.

## Trigger

**Roadmap (Phase B overlay)** — `gaps on` / `enable gaps` / `watch gaps` / `find gaps` (no scope) → § **Mandatory cascade**; buffer on blast-radius traversals; **flush before row `quality`**. Operator does not re-paste watch directive. **Not** `gap analysis on <scope>`. Off: `gaps off`.

## Protocol

See the linked canonical skill. Project-specific overrides go here (rare).
