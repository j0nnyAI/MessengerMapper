---
name: writing-specs
description: Operator says "create a spec", "write a spec", "spec this out", or "spec for [feature]". Converts a settled design into a locked-down implementation spec saved to `02-ARCHITECTURE/specs/`.
allowed-tools: [Read, Write, Edit, Bash, Glob]
---

<!--
FILE: .claude/skills/writing-specs/SKILL.md
PURPOSE: Project-local wrapper for the canonical NEXUS skill.
LINKS: ../../../THE-NEXUS/03-SKILLS/writing-specs.md
GENERATED: by sync-assets-to-template.py. Safe to hand-edit, but a re-run with --force will overwrite.
-->

# Skill: Writing Specs

Wraps `../../../THE-NEXUS/03-SKILLS/writing-specs.md`. Read the canonical file for the full protocol, quality bar, and anti-patterns.

## Trigger

Operator says "create a spec", "write a spec", "spec this out", or "spec for [feature]". Converts a settled design into a locked-down implementation spec saved to `02-ARCHITECTURE/specs/`.

## Protocol

See the linked canonical skill. Project-specific overrides go here (rare).
